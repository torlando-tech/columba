<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
    Android Test Manifest
    Contains test-only components that should not be in the production APK.
    -->

    <application>
        <!-- Test Activity for instrumented UI tests -->
        <activity
            android:name="network.columba.app.test.TestActivity"
            android:exported="false" />
    </application>

    <instrumentation
        android:name="androidx.test.runner.AndroidJUnitRunner"
        android:targetPackage="network.columba.app" />

</manifest>
